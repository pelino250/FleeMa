version: '3'
services:
  web:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - db

  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: fleema_database
    volumes:
      - fleema_data:/var/lib/postgresql

volumes:
  fleema_data: