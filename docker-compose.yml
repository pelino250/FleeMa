services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
    -
    depends_on:
    -
    volumes:
      - .:/app
db:
image: postgre:11-alpine
ports:
  - "5432:5432"
environment:
  -POSTGRE_DB=
      
